#include <iostream>
#include <ctime>
#include <omp.h>
#include <mpi.h>

#define min(A, B) ((A) < (B) ? (A) : (B))
#define max(A, B) ((A) > (B) ? (A) : (B))

const int imax = 80;
const int kmax = 80;
const int itmax = 20000;

int main() {
    // TODO: implement hybrid approach
    return 0;
}
